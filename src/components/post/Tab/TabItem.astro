---
import { cn } from "@/lib/utils";

interface Props {
  label: string; 
  class?: string;
  defaultOpen?: boolean; 
}

const { label, class: className, defaultOpen = false } = Astro.props;
---

<div
  class={cn(
    "sh-builtin-components tab-panel animate-in fade-in duration-300",
    className,
  )}
  data-label={label}
  data-default-open={defaultOpen ? "true" : "false"}
>
  <slot />
</div>
